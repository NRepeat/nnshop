import PlaceholderStringInput from '@/shared/sanity/components/shopify/inputs/PlaceholderString';

const placeholderString = {
  name: 'placeholderString',
  title: 'Title',
  type: 'string',
  components: {
    input: PlaceholderStringInput,
  },
};

export default placeholderString;
