'use server';

export async function createInvoice() {
  const saveData = async () => {
    // await prisma.order.create({
    //   data: {
    //     customerId: '12345', // Example customer ID, replace with actual logic
    //     deliveryId: rawFormData.department as string, // Example delivery ID, replace with actual logic
    //     totalAmount: 100, // Example total amount, replace with actual logic
    //     status: 'PENDING',
    //     orderId: '12345',
    //     amount: 100,
    //   },
    // });
  };
  await saveData();
  // mutate data
  // revalidate the cache
}
