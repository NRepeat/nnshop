// Delivery section removed from checkout receipt sidebar per product decision.
// No delivery cost or delivery address is shown during checkout.

export default async function DeliveryInfoSection({
  locale: _locale,
}: {
  locale: string;
}) {
  return null;
}
