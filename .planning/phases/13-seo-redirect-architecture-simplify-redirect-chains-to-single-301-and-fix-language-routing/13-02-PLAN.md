---
phase: 13
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [next.config.ts]
autonomous: true
requirements: [SEO-13-01]

must_haves:
  truths:
    - "Sanity-managed redirects do not conflict with proxy normalization"
    - "Build completes without errors related to middleware or routing"
  artifacts:
    - path: "next.config.ts"
      provides: "Canonical redirect handling for Sanity-based redirects"
  key_links:
    - from: "next.config.ts"
      to: "proxy.ts"
      via: "Shared canonical URL assumptions (HTTPS, WWW, no trailing slash)"
---

<objective>
Ensure consistency across all redirect sources (Sanity, next.config.ts, and proxy.ts) to prevent any remaining multi-hop chains.

Purpose: Finalize the redirect architecture and verify it works at the build level.
Output: Validated configuration and verification of production-like redirect behavior.
</objective>

<execution_context>
@/Users/mnmac/Development/nnshop/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mnmac/Development/nnshop/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@next.config.ts
@proxy.ts
@src/shared/lib/tests/proxy.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit next.config.ts Redirect Destinations</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts
  </read_first>
  <action>
    Review `next.config.ts` and the `redirects()` function.
    Ensure that any static redirects (if any) use canonical URLs.
    Update the `redirects` mapping to ensure that if a destination is internal (starts with `/`), it is cleaned of trailing slashes to match the middleware's canonical state.
    
    Wiring: Ensure that `next.config.ts` follows the same "Goal State" principles as `proxy.ts`:
    - Destination URLs must be absolute if cross-domain or start with `/` and be slash-normalized if internal.
    - Protocol/Host should ideally be handled by the middleware, but `next.config.ts` should not point to non-www/http versions.
  </action>
  <verify>
    <automated>npx tsc --noEmit next.config.ts && echo "Syntax check passed"</automated>
  </verify>
  <acceptance_criteria>
    - No static non-canonical redirects in `next.config.ts`.
    - Type check for `next.config.ts` passes.
  </acceptance_criteria>
  <done>next.config.ts audited for redirect consistency.</done>
</task>

<task type="auto">
  <name>Task 2: Final Redirect Chain Verification</name>
  <files>src/shared/lib/tests/proxy.test.ts</files>
  <read_first>
    - proxy.ts
    - src/shared/lib/tests/proxy.test.ts
  </read_first>
  <action>
    Add final "End-to-End" unit tests to `proxy.test.ts` that simulate a full production request header set (Host: miomio.com.ua, X-Forwarded-Proto: http) and verify the single-hop 301 response to the final canonical URL.
    
    Test case:
    - `http://miomio.com.ua/uk/` -> `https://www.miomio.com.ua/uk/woman` (single 301)
  </action>
  <verify>
    <automated>npm test src/shared/lib/tests/proxy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests confirm single-hop for all non-canonical variants.
  </acceptance_criteria>
  <done>Final verification completed and tests pass.</done>
</task>

</tasks>

<verification>
Run `npm test src/shared/lib/tests/proxy.test.ts`. Use `npm run build` as a final check if time permits, but unit tests are the primary gate.
</verification>

<success_criteria>
- All structural and domain redirects resolve in ≤ 1 hop in unit tests.
- `next.config.ts` is logically aligned with `proxy.ts` canonicalization.
</success_criteria>

<output>
After completion, create `.planning/phases/13-seo-redirect-architecture-simplify-redirect-chains-to-single-301-and-fix-language-routing/13-02-SUMMARY.md`
</output>
