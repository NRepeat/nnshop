---
phase: 15-seo-schema-markup-add-onlinestore-website-searchaction-itemlist-shipping-and-return-policy-schemas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/seo/jsonld/organization.ts
  - src/shared/lib/seo/jsonld/website.ts
  - src/shared/lib/seo/jsonld/itemlist.ts
  - src/shared/lib/seo/jsonld/webpage.ts
  - src/shared/lib/seo/jsonld/product.ts
  - src/shared/lib/seo/jsonld/index.ts
  - app/[locale]/(frontend)/layout.tsx
  - src/features/collection/ui/CollectionGrid.tsx
  - app/[locale]/(frontend)/info/[slug]/page.tsx
autonomous: true
requirements: [SEO-15-01, SEO-15-02, SEO-15-03, SEO-15-04]
must_haves:
  truths:
    - "Organization and WebSite schemas are present on every page"
    - "Collection pages contain ItemList schema for products"
    - "Static info pages contain WebPage schema"
    - "Product schema includes refined MerchantReturnPolicy and OfferShippingDetails"
  artifacts:
    - path: "src/shared/lib/seo/jsonld/website.ts"
      provides: "WebSite schema with SearchAction"
    - path: "src/shared/lib/seo/jsonld/itemlist.ts"
      provides: "ItemList schema for categories"
    - path: "src/shared/lib/seo/jsonld/webpage.ts"
      provides: "WebPage schema for static content"
    - path: "src/shared/lib/seo/jsonld/organization.ts"
      provides: "Enriched Organization schema with contactPoint"
  key_links:
    - from: "app/[locale]/(frontend)/layout.tsx"
      to: "src/shared/lib/seo/jsonld/organization.ts"
      via: "Organization schema injection"
      pattern: "generateOrganizationJsonLd"
    - from: "app/[locale]/(frontend)/layout.tsx"
      to: "src/shared/lib/seo/jsonld/website.ts"
      via: "WebSite schema injection"
      pattern: "generateWebSiteJsonLd"
    - from: "src/features/collection/ui/CollectionGrid.tsx"
      to: "src/shared/lib/seo/jsonld/itemlist.ts"
      via: "ItemList schema injection"
      pattern: "generateItemListJsonLd"
    - from: "app/[locale]/(frontend)/(product)/product/[slug]/page.tsx"
      to: "src/shared/lib/seo/jsonld/product.ts"
      via: "Product schema with return/shipping policies"
      pattern: "generateProductJsonLd"
    - from: "app/[locale]/(frontend)/info/[slug]/page.tsx"
      to: "src/shared/lib/seo/jsonld/webpage.ts"
      via: "WebPage schema injection"
      pattern: "generateWebPageJsonLd"
---

<objective>
Expand and refine SEO structured data to include Organization (enriched with contacts and social), WebSite (with SearchAction), ItemList (for categories), WebPage (for static pages), and refined Product policies (Shipping/Return) to improve search engine understanding and rich result eligibility.
</objective>

<execution_context>
@/Users/mnmac/Development/nnshop/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mnmac/Development/nnshop/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/shared/lib/seo/jsonld/organization.ts
@src/shared/lib/seo/jsonld/product.ts
@app/[locale]/(frontend)/layout.tsx
@src/features/collection/ui/CollectionGrid.tsx
@app/[locale]/(frontend)/info/[slug]/page.tsx
@.planning/phases/15-seo-schema-markup-add-onlinestore-website-searchaction-itemlist-shipping-and-return-policy-schemas/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define, Expand and Refine Schema Generators</name>
  <files>
    src/shared/lib/seo/jsonld/organization.ts,
    src/shared/lib/seo/jsonld/website.ts,
    src/shared/lib/seo/jsonld/itemlist.ts,
    src/shared/lib/seo/jsonld/webpage.ts,
    src/shared/lib/seo/jsonld/product.ts,
    src/shared/lib/seo/jsonld/index.ts
  </files>
  <action>
    - Update `organization.ts`:
        - Include logo using `SITE_URL` and add all available social links to `sameAs` (`INSTAGRAM_URL`, `FACEBOOK_URL`, `TELEGRAM_URL`, `TELEGRAM_CHANNEL_URL`).
        - Enrich `contactPoint`: add `telephone: "+380972179292"` and `email: "info@miomio.com.ua"`.
    - Create `website.ts`: Implement `generateWebSiteJsonLd(locale: string)` returning `WebSite` schema with `potentialAction` set to `SearchAction`. Target: `${SITE_URL}/${locale}/search?q={search_term_string}`.
    - Create `itemlist.ts`: Implement `generateItemListJsonLd(products: any[], locale: string)` for collection pages. Ensure product URLs are canonical.
    - Create `webpage.ts`: Implement `generateWebPageJsonLd(title: string, description: string, url: string)` for static info pages.
    - Refine `product.ts`:
        - Verify `MerchantReturnPolicy`: ensure `merchantReturnDays` is 14, `returnFees` is `FreeReturn`, and `returnMethod` is `ReturnByMail`. Add `url` field pointing to `${SITE_URL}/${locale}/info/payment-returns`.
        - Verify `OfferShippingDetails`: ensure `shippingRate` for UA is 0 (free shipping), and `deliveryTime` reflects 1-3 days transit time.
    - Export all new generators from `index.ts`.
  </action>
  <verify>
    <automated>grep -E "generateWebSiteJsonLd|generateItemListJsonLd|generateWebPageJsonLd" src/shared/lib/seo/jsonld/index.ts</automated>
  </verify>
  <done>
    All schema generator functions exist, are exported, and contain enriched/refined data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Schemas into UI Components</name>
  <files>
    app/[locale]/(frontend)/layout.tsx,
    src/features/collection/ui/CollectionGrid.tsx,
    app/[locale]/(frontend)/info/[slug]/page.tsx
  </files>
  <action>
    - `layout.tsx`: Add `JsonLd` for `WebSite` schema alongside existing Organization schema.
    - `CollectionGrid.tsx`: Add `JsonLd` for `ItemList` schema using products from the collection result.
    - `app/[locale]/(frontend)/info/[slug]/page.tsx`: Add `JsonLd` for `WebPage` schema using page title and description.
  </action>
  <verify>
    <automated>grep -E "generateWebSiteJsonLd|generateItemListJsonLd|generateWebPageJsonLd" app/[locale]/(frontend)/layout.tsx src/features/collection/ui/CollectionGrid.tsx app/[locale]/(frontend)/info/\[slug\]/page.tsx</automated>
  </verify>
  <done>
    All requested schemas are rendered in the HTML of their respective pages.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Vitest suite for Schema Validation</name>
  <files>src/shared/lib/tests/schema.test.ts</files>
  <action>
    Create a Vitest suite to verify that:
    1. `generateWebSiteJsonLd` returns valid SearchAction structure.
    2. `generateItemListJsonLd` correctly maps product list to ItemList structure.
    3. `generateWebPageJsonLd` includes basic WebPage properties.
    4. Organization schema contains all expected social links and enriched contact point (phone/email).
    5. `generateProductJsonLd` includes correct return policy (14 days, link to policy) and shipping details (Free, 1-3 days).
  </action>
  <verify>
    <automated>npm test src/shared/lib/tests/schema.test.ts</automated>
  </verify>
  <done>
    Unit tests verify schema correctness and data fidelity.
  </done>
</task>

</tasks>

<success_criteria>
1. Organization schema includes enriched contact point and all social profiles.
2. WebSite schema includes functional SearchAction target.
3. ItemList schema correctly lists products on category pages.
4. WebPage schema present on info pages (About, Delivery, etc.).
5. Product schema includes refined MerchantReturnPolicy and OfferShippingDetails.
6. All schemas are syntactically valid JSON-LD and verified via Vitest.
</success_criteria>

<output>
After completion, create `.planning/phases/15-seo-schema-markup-add-onlinestore-website-searchaction-itemlist-shipping-and-return-policy-schemas/15-01-SUMMARY.md`
</output>
