---
phase: 16-seo-image-alt-text
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/seo/getImageAlt.ts
  - src/shared/lib/seo/getImageAlt.test.ts
  - src/shared/sanity/schemaTypes/blocks/heroType.ts
  - src/shared/sanity/lib/query.ts
  - src/entities/product/ui/ProductCard.tsx
  - src/entities/product/ui/ProductCardSPP.tsx
  - src/entities/product/ui/ProductQuickView.tsx
  - src/features/product/ui/Gallery.tsx
  - src/features/cart/ui/CartItem.tsx
  - src/features/favorites/ui/FavoriteProductCard.tsx
  - src/features/order/ui/OrderCard.tsx
  - src/features/order/ui/OrderDetails.tsx
  - src/features/checkout/receipt/ui/OrderSummary.tsx
autonomous: true
requirements: [SEO-16-01, SEO-16-02, SEO-16-03]
must_haves:
  truths:
    - "Product images render with alt text format: '{Product Title} {Variant Info}' (per user decision)"
    - "Sanity content images render custom alt text if provided in Studio, or fallback gracefully"
    - "Cart, Favorites, and Order pages provide descriptive alt text for product images"
    - "Alt text is correctly translated based on current locale (UK/RU)"
    - "Decorative images explicitly have alt=\"\""
  artifacts:
    - path: "src/shared/lib/seo/getImageAlt.ts"
      provides: "getProductAlt utility for descriptive, localized alt text"
    - path: "src/shared/sanity/lib/query.ts"
      provides: "Updated GROQ queries fetching the 'alt' field"
  key_links:
    - from: "src/entities/product/ui/ProductCard.tsx"
      to: "src/shared/lib/seo/getImageAlt.ts"
      via: "import and call getProductAlt"
      pattern: "import.*getProductAlt.*from.*@/shared/lib/seo/getImageAlt"
    - from: "src/shared/sanity/components/portableText/image.tsx"
      to: "src/shared/sanity/lib/query.ts"
      via: "fetches and renders 'alt' from Sanity image data"
      pattern: "alt={.*alt.*}"
---

<objective>
Implement descriptive and localized alt text for all product and content images to improve SEO and accessibility. This involves creating a centralized utility for product images, updating Sanity schemas and GROQ queries, and ensuring full coverage across all product surfaces (including Cart, Favorites, and Orders).
</objective>

<execution_context>
@/Users/mnmac/Development/nnshop/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/entities/product/ui/ProductCard.tsx
@src/features/product/ui/Gallery.tsx
@src/shared/sanity/lib/query.ts
@src/shared/sanity/schemaTypes/blocks/heroType.ts
@.planning/phases/16-seo-image-alt-text/CONTEXT.md
@.planning/phases/16-seo-image-alt-text/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Image Alt Utility and Test Suite</name>
  <files>
    src/shared/lib/seo/getImageAlt.ts,
    src/shared/lib/seo/getImageAlt.test.ts
  </files>
  <action>
    - Implement `getProductAlt(product: any, variant: any, locale: string)`:
      - Construct string: `{product.title} {variant?.title || ''}`.
      - Ensure fallback to `{product.title}` if variant is missing.
      - Handle localization for any hardcoded strings if necessary (though title/variant should be localized from Shopify).
    - Create Vitest suite `getImageAlt.test.ts` to verify the logic for various product/variant shapes.
  </action>
  <verify>
    <automated>npm test src/shared/lib/seo/getImageAlt.test.ts</automated>
  </verify>
  <done>
    Utility correctly formats alt text based on product and variant info.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Sanity Schemas and GROQ Queries</name>
  <files>
    src/shared/sanity/schemaTypes/blocks/heroType.ts,
    src/shared/sanity/lib/query.ts
  </files>
  <action>
    - Add `alt` field to `heroType.ts` and any other image-containing schemas if missing.
    - Update `src/shared/sanity/lib/query.ts`:
      - For all queries returning image objects (`POSTS_QUERY`, `PAGE_QUERY`, `HOME_PAGE`, etc.), ensure `alt` is fetched from the image field.
      - Use `coalesce(image.alt[$language], image.alt.uk, image.alt.ru)` where appropriate for localized fields.
  </action>
  <verify>
    <automated>grep -E "alt.*coalesce" src/shared/sanity/lib/query.ts</automated>
  </verify>
  <done>
    Sanity content fetches alt text from the CMS with localization support.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate into All Product Surfaces</name>
  <files>
    src/entities/product/ui/ProductCard.tsx,
    src/entities/product/ui/ProductCardSPP.tsx,
    src/entities/product/ui/ProductQuickView.tsx,
    src/features/product/ui/Gallery.tsx,
    src/features/cart/ui/CartItem.tsx,
    src/features/favorites/ui/FavoriteProductCard.tsx,
    src/features/order/ui/OrderCard.tsx,
    src/features/order/ui/OrderDetails.tsx,
    src/features/checkout/receipt/ui/OrderSummary.tsx
  </files>
  <action>
    - Update all `<Image />` components on product surfaces to use the descriptive alt text utility.
    - **CRITICAL**: Use the non-destructive pattern: `alt={image.altText || getProductAlt(product, variant, locale)}`.
    - Ensure both main images and thumbnails in `Gallery.tsx` are updated.
    - Update Cart, Favorites, and Orders components to ensure consistent alt text for product thumbnails.
  </action>
  <verify>
    <automated>grep -E "getProductAlt" src/entities/product/ui/ProductCard.tsx src/features/cart/ui/CartItem.tsx src/features/favorites/ui/FavoriteProductCard.tsx</automated>
  </verify>
  <done>
    All user-facing product images have descriptive alt text with hierarchical fallback.
  </done>
</task>

<task type="auto">
  <name>Task 4: Decorative Image Audit</name>
  <files>
    src/**
  </files>
  <action>
    - Audit all usages of `<Image />` in the codebase.
    - Ensure purely decorative images (e.g. icons that repeat text labels, background patterns) explicitly have `alt=""`.
    - Fix any cases where `alt` is missing or undefined (which causes screen readers to read the URL).
  </action>
  <verify>
    <automated>grep -r "<Image" src --include="*.tsx" | grep -v "alt=" || echo "Some images missing alt attribute"</automated>
  </verify>
  <done>
    Every image in the application has an appropriate alt attribute (either descriptive or empty for decorative).
  </done>
</task>

</tasks>

<success_criteria>
1. `getProductAlt` utility correctly formats alt text as "{Title} {Variant}" and is verified by tests.
2. GROQ queries in `query.ts` are updated to fetch localized `alt` fields from Sanity.
3. All critical product surfaces (Cards, Gallery, Cart, Favorites, Orders) render descriptive alt text.
4. Fallback logic `image.altText || getProductAlt(...)` is used everywhere.
5. All images have either meaningful alt text or `alt=""`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-seo-image-alt-text/16-01-SUMMARY.md`
</output>
