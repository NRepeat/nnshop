---
phase: 06-ui-polish
plan: 02
subsystem: ui
tags: [sanity, cms, footer, groq, sanity-typegen, i18n, server-component]

# Dependency graph
requires:
  - phase: 06-ui-polish
    provides: phase context and UI polish objective
provides:
  - footerSettings Sanity schema with socialLinks, workingHours, address, paymentMethods fields
  - FOOTER_QUERY GROQ query for fetching footer CMS data from siteSettings
  - Footer component as async Server Component fetching and rendering CMS-managed content conditionally
affects: [06-ui-polish, any plan using Footer component or siteSettings Sanity data]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sanity CMS-driven footer: schema fields -> GROQ query -> async Server Component rendering"
    - "Conditional CMS rendering: sections only render when data is non-empty (graceful degradation)"
    - "Locale-aware CMS fields: object with uk/ru keys, access via locale as 'uk' | 'ru'"

key-files:
  created: []
  modified:
    - src/shared/sanity/schemaTypes/shopify/shemas/objects/global/footer.ts
    - src/shared/sanity/schemaTypes/siteSettingsType.ts
    - src/shared/sanity/lib/query.ts
    - src/widgets/footer/ui/Footer.tsx

key-decisions:
  - "Explicit type annotations for socialLinks map callback (link: { platform?: string | null; url?: string | null }) because Sanity typegen not run — avoids implicit any without as-any cast"
  - "sanityFetch import from @shared/sanity/lib/client (not lib/fetch) — consistent with Header component pattern in codebase"
  - "hasContactColumn check includes workingHours, address, and socialLinks — column hidden when all three are empty"
  - "payment method span key uses method value (string) not index — avoids React reconciliation issues on reorder"

patterns-established:
  - "Footer CMS pattern: schema type -> siteSettings field -> FOOTER_QUERY -> sanityFetch in async Footer component"
  - "Graceful CMS degradation: hasContactColumn / hasPaymentMethods boolean guards prevent empty column rendering"

requirements-completed: [UI-03]

# Metrics
duration: 2min
completed: 2026-02-26
---

# Phase 6 Plan 02: CMS-managed Footer Summary

**Footer expanded from static 2-column to CMS-driven 3-column layout with social links, working hours, address, and payment method badges — all conditional on Sanity Studio configuration**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-26T13:40:24Z
- **Completed:** 2026-02-26T13:42:21Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- footerSettings Sanity schema expanded with socialLinks array, workingHours (uk/ru), address (uk/ru), and paymentMethods array
- footerSettings registered as field in siteSettingsType — visible in Sanity Studio under Site Settings
- FOOTER_QUERY exported from query.ts fetching all 4 footerSettings sub-fields via GROQ
- Footer.tsx converted to async Server Component that fetches CMS data and conditionally renders third column + payment badges

## Task Commits

Each task was committed atomically:

1. **Task 1: Expand footerSettings schema and register in siteSettings** - `8d77e69` (feat)
2. **Task 2: Add FOOTER_QUERY to query.ts** - `cd48de8` (feat)
3. **Task 3: Update Footer component to fetch and render CMS data** - `ac47d0a` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src/shared/sanity/schemaTypes/shopify/shemas/objects/global/footer.ts` - Replaced single text block with socialLinks, workingHours, address, paymentMethods fields
- `src/shared/sanity/schemaTypes/siteSettingsType.ts` - Added footerSettings field after brandsNavigation
- `src/shared/sanity/lib/query.ts` - Added FOOTER_QUERY with defineQuery for footerSettings sub-fields
- `src/widgets/footer/ui/Footer.tsx` - Made async, added sanityFetch for FOOTER_QUERY, conditional third column and payment badges

## Decisions Made
- Explicit type annotations for socialLinks map callback because Sanity typegen not run — `{ platform?: string | null; url?: string | null }` avoids implicit any without `as any` cast
- sanityFetch imported from `@shared/sanity/lib/client` (not `lib/fetch`) — matches Header component import pattern
- hasContactColumn boolean derived from presence of any workingHours/address/socialLinks data — column stays hidden when all empty
- payment method tags use `footer!.paymentMethods!.map((method: string | null)` — non-null assertion safe because `hasPaymentMethods` guard already confirmed presence

## Deviations from Plan

**1. [Rule 1 - Bug] Explicit type annotations for map callbacks**
- **Found during:** Task 3 (Footer component update)
- **Issue:** TypeScript strict mode reported implicit `any` type for `link` and `method` parameters in `.map()` callbacks because FOOTER_QUERY type hasn't been generated by `npm run typegen` yet
- **Fix:** Added explicit inline type annotation `(link: { platform?: string | null; url?: string | null })` and `(method: string | null)` to map callbacks
- **Files modified:** src/widgets/footer/ui/Footer.tsx
- **Verification:** `npx tsc --noEmit` reports no Footer.tsx errors; zero `as any` casts
- **Committed in:** ac47d0a (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - TypeScript strict mode compliance)
**Impact on plan:** Fix required for TypeScript correctness; no scope creep. Will be superseded when `npm run typegen` is next run.

## Issues Encountered
- Sanity typegen not run for new FOOTER_QUERY — generated types file does not yet include FOOTER_QUERYResult type. Resolved with explicit type annotations. Next typegen run will generate full types automatically.

## User Setup Required
None - no external service configuration required. To configure footer content: open Sanity Studio -> Site Settings -> Footer and populate the fields.

## Next Phase Readiness
- Footer now CMS-managed; client can populate social links, hours, address, and payment methods in Sanity Studio
- Plan 06-03 can proceed with remaining UI polish tasks
- Running `npm run typegen` will generate FOOTER_QUERYResult type and allow removing explicit type annotations

---
*Phase: 06-ui-polish*
*Completed: 2026-02-26*
