---
phase: 03-visible-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/sanity/schemaTypes/blocks/info-bar.ts
  - src/shared/sanity/lib/query.ts
  - src/shared/sanity/types.ts
  - src/entities/announcement-bar/announcement-bar.tsx
autonomous: true
requirements:
  - BUG-02

must_haves:
  truths:
    - "The Viber link in the announcement bar opens a real phone number, not a placeholder"
    - "If no viberPhone is set in Sanity and no VIBER_PHONE_NUMBER env var exists, the Viber link is hidden entirely"
    - "The rest of the announcement bar (text, other links) still renders when Viber link is hidden"
    - "Phone number stored in Sanity as raw digits constructs the correct viber://chat?number=%2B{digits} URL"
  artifacts:
    - path: "src/shared/sanity/schemaTypes/blocks/info-bar.ts"
      provides: "viberPhone field in Sanity schema"
      contains: "viberPhone"
    - path: "src/shared/sanity/lib/query.ts"
      provides: "viberPhone in HEADER_QUERY projection"
      contains: "viberPhone"
    - path: "src/entities/announcement-bar/announcement-bar.tsx"
      provides: "Conditional Viber link with real phone number"
      contains: "viber://chat?number=%2B"
  key_links:
    - from: "src/shared/sanity/schemaTypes/blocks/info-bar.ts"
      to: "src/shared/sanity/types.ts"
      via: "npm run typegen"
      pattern: "viberPhone"
    - from: "src/shared/sanity/lib/query.ts"
      to: "src/entities/announcement-bar/announcement-bar.tsx"
      via: "HEADER_QUERYResult infoBar projection"
      pattern: "viberPhone"
---

<objective>
Fix the hardcoded Viber placeholder in the announcement bar. Currently the component renders `viber://chat?number=%2B380XXXXXXXXX` unconditionally. This fix adds a `viberPhone` field to the Sanity `infoBar` schema, exposes it via GROQ query, regenerates types, and updates the component to use the real phone number with an env var fallback — hiding the link entirely when neither source provides a value.

Purpose: The announcement bar Viber link must open a real phone number for customers to contact the shop.
Output: Updated Sanity schema, updated GROQ query, regenerated types, updated announcement-bar component.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viberPhone to Sanity schema and GROQ query, then regenerate types</name>
  <files>
    src/shared/sanity/schemaTypes/blocks/info-bar.ts
    src/shared/sanity/lib/query.ts
    src/shared/sanity/types.ts
  </files>
  <action>
    **Step 1 — Add `viberPhone` field to Sanity schema:**

    Read `src/shared/sanity/schemaTypes/blocks/info-bar.ts`. Find the `telephone` field definition. Add a new `viberPhone` field immediately after `telephone`:

    ```typescript
    {
      name: 'viberPhone',
      type: 'string',
      title: 'Viber Phone Number (digits only)',
      description: 'Raw digits for Viber deep link, e.g. 380991234567. No +, no spaces.',
    },
    ```

    **Step 2 — Add `viberPhone` to HEADER_QUERY projection:**

    Read `src/shared/sanity/lib/query.ts`. Find the `HEADER_QUERY` constant and locate the `infoBar` projection block. Add `viberPhone` alongside `telephone` in the projection:

    ```groq
    infoBar {
      ...,
      telephone,
      viberPhone,
      ...rest of existing fields
    }
    ```

    The exact surrounding context may differ. Match the indentation and style of the existing `telephone` line. Add `viberPhone` on the next line after `telephone`.

    **Step 3 — Regenerate Sanity types:**

    Run:
    ```bash
    npm run typegen
    ```

    This regenerates `src/shared/sanity/types.ts`. Do NOT manually edit `types.ts`. The `HEADER_QUERYResult` type will automatically include `viberPhone?: string | null` after regeneration.

    Verify: after running typegen, check that `src/shared/sanity/types.ts` contains the string `viberPhone`.
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npm run typegen 2>&1 | tail -10 && grep -n "viberPhone" src/shared/sanity/types.ts | head -5</automated>
    <manual>Confirm `viberPhone` appears in types.ts after typegen completes.</manual>
  </verify>
  <done>
    - `info-bar.ts` schema contains `viberPhone` field definition
    - `query.ts` HEADER_QUERY `infoBar` projection includes `viberPhone`
    - `types.ts` contains `viberPhone` (generated by typegen, not manually edited)
    - `npm run typegen` exits with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update announcement-bar component to use viberPhone with env fallback</name>
  <files>src/entities/announcement-bar/announcement-bar.tsx</files>
  <action>
    Read `src/entities/announcement-bar/announcement-bar.tsx`. This is a Server Component.

    The current component has a hardcoded `href="viber://chat?number=%2B380XXXXXXXXX"`. Replace this entire Viber link section with dynamic logic.

    **In the component body (before the return statement), add:**

    ```typescript
    const resolvedViberPhone = props.viberPhone || process.env.VIBER_PHONE_NUMBER || null;
    const viberUrl = resolvedViberPhone
      ? `viber://chat?number=%2B${resolvedViberPhone}`
      : null;
    ```

    Where `props` is whatever the component receives. If `viberPhone` is destructured from props, use the destructured variable. Adapt to the existing code structure.

    **Replace the hardcoded Viber `<a>` element:**

    Find the element with `href="viber://chat?number=%2B380XXXXXXXXX"` (or similar hardcoded pattern). Replace it with a conditional:

    ```tsx
    {viberUrl && (
      <a href={viberUrl} target="_blank" rel="noopener noreferrer">
        {/* preserve the existing Viber icon JSX exactly as-is */}
      </a>
    )}
    ```

    Keep all existing classes, icon content, and surrounding markup unchanged — only make the `href` dynamic and wrap in `{viberUrl && ...}`.

    **Environment variable:** The env var fallback is `VIBER_PHONE_NUMBER`. No `NEXT_PUBLIC_` prefix — AnnouncementBar is a Server Component and the number must not be exposed to the client bundle.

    **Source priority per locked decision:**
    1. `viberPhone` from Sanity (primary)
    2. `VIBER_PHONE_NUMBER` env var (fallback)
    3. If both empty/null: `viberUrl` is null → link is hidden entirely
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npm run build 2>&1 | tail -20</automated>
    <manual>
      1. Run dev server and navigate to any page showing the announcement bar
      2. Without env var set: Viber link should be absent (hidden), rest of bar renders normally
      3. With `VIBER_PHONE_NUMBER=380991234567` in .env.local: Viber link should appear with href `viber://chat?number=%2B380991234567`
    </manual>
  </verify>
  <done>
    - `announcement-bar.tsx` uses `props.viberPhone || process.env.VIBER_PHONE_NUMBER || null` to resolve the phone
    - Viber link is conditionally rendered: only present when `viberUrl` is non-null
    - No hardcoded `380XXXXXXXXX` placeholder remains in the file
    - `npm run build` passes with zero TypeScript errors
  </done>
</task>

</tasks>

<verification>
Run `npm run build` — must pass with zero new errors.
Confirm no hardcoded `380XXXXXXXXX` remains in `announcement-bar.tsx`.
Confirm `viberPhone` appears in `src/shared/sanity/types.ts`.
</verification>

<success_criteria>
- `viberPhone` field exists in `info-bar.ts` Sanity schema
- `viberPhone` field appears in `HEADER_QUERY` `infoBar` projection in `query.ts`
- `src/shared/sanity/types.ts` contains `viberPhone` (from typegen, not manual edit)
- `announcement-bar.tsx` resolves phone from Sanity `viberPhone` → `VIBER_PHONE_NUMBER` env → null
- Viber `<a>` element only renders when phone is resolved (hidden when both sources are empty)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-visible-bug-fixes/03-02-SUMMARY.md`
</output>
