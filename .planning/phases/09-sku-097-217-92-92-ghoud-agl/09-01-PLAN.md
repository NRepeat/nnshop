---
phase: 09-sku-097-217-92-92-ghoud-agl
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/checkout/receipt/ui/OrderSummary.tsx
  - src/features/checkout/payment/ui/Payment.tsx
autonomous: true
requirements:
  - PHASE9-CHECKOUT-DELIVERY-REMOVAL

must_haves:
  truths:
    - "Receipt sidebar shows no shipping line — only subtotal (+ discount if applied) and total"
    - "Grand total in OrderSummary equals totalAmount (no shippingFee added)"
    - "Payment.tsx cartAmount equals goodsTotal (no shippingFee added)"
  artifacts:
    - path: "src/features/checkout/receipt/ui/OrderSummary.tsx"
      provides: "Order summary without shipping fee row"
      contains: "const grandTotal = totalAmount"
    - path: "src/features/checkout/payment/ui/Payment.tsx"
      provides: "Payment amount without shipping fee"
      contains: "cartAmount = goodsTotal"
  key_links:
    - from: "OrderSummary grandTotal"
      to: "totalAmount (no shippingFee)"
      via: "direct assignment"
      pattern: "const grandTotal = totalAmount"
---

<objective>
Remove the delivery/shipping price calculation from the checkout receipt sidebar and payment amount.

Purpose: User decided to remove the shipping fee from the order total. The receipt should show subtotal → (discount if applied) → total, with no shipping line. The payment amount must also not include the shipping fee.
Output: OrderSummary.tsx without shippingFee row, Payment.tsx without shippingFee in cartAmount.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sku-097-217-92-92-ghoud-agl/09-CONTEXT.md
@.planning/phases/09-sku-097-217-92-92-ghoud-agl/09-RESEARCH.md
</context>

<interfaces>
From src/features/checkout/receipt/ui/OrderSummary.tsx (lines 181-183, 217-222):
```typescript
// Current — REMOVE these:
const shippingFee = Math.round(20 + totalAmount * 0.02);
const grandTotal = totalAmount + shippingFee;

// Replace with:
const grandTotal = totalAmount;

// Current shipping display row (lines 217-222) — REMOVE entirely:
<div className="flex justify-between text-sm">
  <span className="text-gray-500">{t('shipping')}</span>
  <span className="text-gray-500">
    {formatPrice(shippingFee)} {currencySymbol}
  </span>
</div>
```

From src/features/checkout/payment/ui/Payment.tsx (lines 58-60):
```typescript
// Current — REMOVE these two lines:
const shippingFee = Math.round(20 + goodsTotal * 0.02);
cartAmount = goodsTotal + shippingFee;

// Replace with:
cartAmount = goodsTotal;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove shipping fee from OrderSummary receipt sidebar</name>
  <files>src/features/checkout/receipt/ui/OrderSummary.tsx</files>
  <action>
    1. Remove the `shippingFee` variable: delete `const shippingFee = Math.round(20 + totalAmount * 0.02);`
    2. Change `const grandTotal = totalAmount + shippingFee;` to `const grandTotal = totalAmount;`
    3. Remove the entire shipping display `<div>` block (lines ~217-222):
       ```tsx
       <div className="flex justify-between text-sm">
         <span className="text-gray-500">{t('shipping')}</span>
         <span className="text-gray-500">
           {formatPrice(shippingFee)} {currencySymbol}
         </span>
       </div>
       ```
    No other changes to this file.
  </action>
  <verify>
    `grep -n "shippingFee" src/features/checkout/receipt/ui/OrderSummary.tsx` — should return no results.
    `grep -n "grandTotal" src/features/checkout/receipt/ui/OrderSummary.tsx` — should show `const grandTotal = totalAmount`.
  </verify>
  <done>OrderSummary shows no shipping row. grandTotal equals totalAmount directly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove shipping fee from Payment.tsx cart amount</name>
  <files>src/features/checkout/payment/ui/Payment.tsx</files>
  <action>
    1. Remove `const shippingFee = Math.round(20 + goodsTotal * 0.02);` (line ~59).
    2. Change `cartAmount = goodsTotal + shippingFee;` to `cartAmount = goodsTotal;` (line ~60).
    No other changes to this file.
  </action>
  <verify>
    `grep -n "shippingFee" src/features/checkout/payment/ui/Payment.tsx` — should return no results.
    `grep -n "cartAmount = goodsTotal" src/features/checkout/payment/ui/Payment.tsx` — should match.
  </verify>
  <done>Payment.tsx cartAmount equals goodsTotal with no shipping added.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors in modified files.
2. `npm run lint` — no lint errors.
3. `grep -rn "shippingFee" src/features/checkout/` — returns no results.
4. Receipt sidebar shows: items, subtotal, optional discount row, total (no shipping row).
</verification>

<success_criteria>
- shippingFee variable removed from both OrderSummary.tsx and Payment.tsx
- grandTotal = totalAmount (no shipping added) in OrderSummary
- cartAmount = goodsTotal (no shipping added) in Payment.tsx
- Shipping row absent from receipt sidebar UI
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-sku-097-217-92-92-ghoud-agl/09-01-SUMMARY.md`
</output>
