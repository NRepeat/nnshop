---
phase: 09-sku-097-217-92-92-ghoud-agl
task: checkpoint-06
total_tasks: 6
status: in_progress
last_updated: 2026-03-04T11:10:36.529Z
---

<current_state>
Phase 09 code plans 01-05 complete. 09-06 is the human CMS/admin checkpoint. This session fixed 3 bugs (committed as 1506081). One concern remains: "синие края" (blue edges) on card — may be resolved after Card wrapper removal, needs user verification.
</current_state>

<completed_work>

- 09-01 through 09-05: All SUMMARY.md + commits ✓
- BONUS fixes (prev session): NovaPoshta URL params, geolocation race, mobile height, discount scaling ✓

**This session (committed 1506081):**
- BUG #1 FIX: PaymentForm.tsx — resetCartSession() wrapped in try-catch, failure no longer blocks router.push to success page
- BUG #2 FIX: src/shared/ui/input.tsx — appearance-none + webkit-autofill box-shadow override (fixes Safari blue bg + roundings)
- SUCCESS PAGE: Thank.tsx — fetches order line items via getOrderById(shopifyOrderId, locale) and displays them (image, title, variant, qty, price, total)
- SUCCESS PAGE: @receipt/success/[orderId]/page.tsx — removed Card wrapper + hidden md:flex; ContactCard/DeliveryCard/PaymentCard now visible on mobile

</completed_work>

<remaining_work>

- Verify fixes in browser (mobile + Safari)
- "Синие края" — user reported blue card edges; removed Card/ui-container-primary wrapper from SuccessReceipt, may be fixed; if not, investigate CheckoutStepper ring-primary color
- 09-06 CHECKPOINT (human tasks):
  1. Sanity Studio: infoBar phone → 097 217 92 92 + viberPhone field
  2. Sanity Studio: GHOUD + AGL brand logos in home page brandGridBlock
  3. Shopify Admin: verify navigation menu
  4. Test "Стежити за ціною" end-to-end
- After 09-06 confirmed: run /gsd:verify-work 9, then mark phase complete

</remaining_work>

<decisions_made>

- getOrderById used (not getOrder) — fetches via PRICE_APP_URL external API, includes product images via fetchProductImages, accepts GID or numeric ID
- resetCartSession failure is non-blocking — order already in Shopify
- appearance-none added globally to Input component (correct)
- "Разом" hardcoded as total label in Thank.tsx (i18n key doesn't exist yet)

</decisions_made>

<blockers>

- NovaPoshta widget 401: domains need whitelisting in business.novapost.com
- 09-06: requires manual CMS/admin work by user

</blockers>

<context>
Unstaged user changes (not touched):
- src/entities/checkout/ui/CheckoutStepper.tsx (user rewrote)
- src/features/header/navigation/ui/PersistLinkNavigation.tsx (user added prefetch)
- src/features/novaPoshta/ui/NovaPoshtaButton.module.css (user set height 70vh)
</context>

<next_action>
Test fixes in browser. If "синие края" still visible, check if CheckoutStepper ring-primary (dark navy oklch(21.64% 0.049 276.34)) is the culprit — consider removing ring-2 ring-primary ring-offset-2 from active step. Then proceed to 09-06 human checkpoint.
</next_action>
