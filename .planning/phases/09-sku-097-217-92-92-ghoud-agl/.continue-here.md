---
phase: 09-sku-097-217-92-92-ghoud-agl
task: checkpoint-06
total_tasks: 6
status: in_progress
last_updated: 2026-03-04T10:20:24.024Z
---

<current_state>
Phase 09 execution is underway. Plans 09-01 through 09-05 are complete (all have SUMMARY.md + commits). 09-06 is a human checkpoint (CMS/admin tasks). Several bonus fixes were made during execution based on user-reported bugs. Two bugs remain open: (1) thank you page not showing order, (2) Safari strange roundings and blue background.
</current_state>

<completed_work>

- 09-01: Removed shipping fee from OrderSummary receipt sidebar and Payment.tsx cartAmount ✓
- 09-02: OrderStatusBadge green/red for Ukrainian+Russian strings; NovaPoshtaButton console cleanup ✓
- 09-03: Cart auto-opens on add-to-cart; QuickBuyModal phone defaults to '+38' ✓
- 09-04: getProductsBySku API created; SKU-based related products tier in ProductSessionView; console.logs removed ✓
- 09-05: DiscountCodeInput embedded in checkout OrderSummary sidebar ✓
- BONUS fix: create.ts discount codes — proportional price scaling so Shopify order total matches payment (eeb510f)
- BONUS fix: NovaPoshtaButton — refactored to use URL query params (not postMessage) per NovaPoshta docs (7eb38dd)
- BONUS fix: NovaPoshta geolocation race condition — re-send coords if resolved after modal opens (4e219f5)
- BONUS fix: NovaPoshta mobile modal height — 90dvh bottom sheet (d536b27)
- BONUS fix: NovaPoshta placeName '' when coords available so map centers on user location (ed64c66)
- BONUS: Added NEXT_PUBLIC_NP_WIDGET_API_KEY=d1592ccb3347ececda1227ccac626e54 to .env.local
- BONUS debug: [NP] logging added throughout NovaPoshtaButton (921c5d9)

</completed_work>

<remaining_work>

- 09-06 CHECKPOINT: Human CMS/admin tasks still pending:
  1. Sanity Studio: update infoBar phone to 097 217 92 92 + add Viber icon/link
  2. Sanity Studio: add GHOUD and AGL brand logos in home page brand grid block
  3. Shopify Admin: verify navigation menu structure
  4. Manual test: "Стежити за ціною" price tracking end-to-end

- BUG #1: Thank you page не відображає замовлення after latest updates
  - `Thank.tsx` at `src/entities/thank-page/ui/Thank.tsx` looks up order from DB by orderName
  - `PaymentForm.tsx` line 91: `orderName = createdOrder.name.replace('#', '')` → redirect to `/checkout/success/${orderName}`
  - DB lookup: `{ orderName: { contains: searchId } }` — should match `#1234` when searchId is `1234`
  - Need to investigate: maybe DB save fails silently, or something in latest updates broke it
  - Likely candidate: `DiscountCodeInput` server component rendering error on payment step causing OrderSummary to fail, but that shouldn't affect Thank.tsx

- BUG #2: Safari — strange roundings and blue background
  - Reported after recent updates
  - Likely suspect: `DiscountCodeInput` added to `OrderSummary` in 09-05 — Safari renders inputs with blue autofill/focus styles
  - Also check: `input[type="hidden"]` in `NovaPoshtaButton` Safari rendering
  - Files to check: `src/features/cart/ui/DiscountCodeInput.tsx`, `src/features/checkout/receipt/ui/OrderSummary.tsx`

- After bugs fixed: run verifier for phase 09, mark complete
- After 09-06 checkpoint confirmed: run verification step, then mark phase complete

</remaining_work>

<decisions_made>

- NovaPoshta widget: params go in URL query string (not postMessage) — per official NovaPoshta docs
- NovaPoshta API key: d1592ccb3347ececda1227ccac626e54 (added to .env.local as NEXT_PUBLIC_NP_WIDGET_API_KEY)
- Discount in Shopify order: proportional price scaling approach (no Draft Order API refactor needed) — scales all line item prices by goodsTotal/localTotal ratio
- Discount code also written to order note as "Промокод: CODE" for admin visibility

</decisions_made>

<blockers>

- NovaPoshta widget 401: domain `nmactunel.nninc.uk` (tunnel) + `miomio.com.ua` / `www.miomio.com.ua` need to be whitelisted in NovaPoshta widget account (business.novapost.com). This is a config task, not code.
- 09-06 checkpoint: requires manual CMS/admin work by user

</blockers>

<context>
Phase 09 is about fixes: checkout receipt, order badges, cart UX, SKU-related products, discount code in checkout, and CMS/admin updates. The code plans (01-05) are all done. The NovaPoshta widget got extensive fixes beyond the original plan scope. Two bugs reported by user are unresolved: thank you page order display and Safari visual glitches. These need investigation before closing the phase and running the verifier.

Unstaged changes exist:
- .planning/phases/09-sku-097-217-92-92-ghoud-agl/09-05-SUMMARY.md (minor)
- src/entities/checkout/ui/CheckoutStepper.tsx (user modified)
- src/features/header/navigation/ui/PersistLinkNavigation.tsx (user modified)
- src/features/novaPoshta/ui/NovaPoshtaButton.module.css (user modified CSS height values)
</context>

<next_action>
Start with: Investigate BUG #1 (thank you page) — check `src/entities/thank-page/ui/Thank.tsx` and `src/features/checkout/payment/ui/PaymentForm.tsx` lines 88-110. Add a console.log in Thank.tsx to verify orderId lookup. Then fix BUG #2 (Safari) — check DiscountCodeInput for missing `appearance-none` or autofill CSS reset. After both bugs fixed, complete 09-06 checkpoint with user, then run `/gsd:verify-work 9`.
</next_action>
