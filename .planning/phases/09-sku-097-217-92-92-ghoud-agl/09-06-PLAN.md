---
phase: 09-sku-097-217-92-92-ghoud-agl
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - PHASE9-PHONE-VIBER
  - PHASE9-BRAND-LOGOS
  - PHASE9-NAVIGATION-MENU
  - PHASE9-PRICE-TRACKING-TEST

must_haves:
  truths:
    - "AnnouncementBar shows phone 097 217 92 92"
    - "AnnouncementBar shows Viber icon linking to 097 217 92 92"
    - "Brand logos for GHOUD and AGL appear in the home page brand grid"
    - "Navigation menu reflects the intended structure"
    - "'Стежити за ціною' feature works end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Human-verify and complete the CMS/admin tasks: update phone + Viber in Sanity, add GHOUD and AGL brand logos in Sanity Studio, verify navigation menu, and test the "стежити за ціною" price tracking feature.

Purpose: These tasks require direct Sanity Studio and Shopify Admin interaction — no code changes. The phone/Viber update is in Sanity CMS (infoBar document). Brand logos are CMS content. Navigation is managed in Shopify admin. Price tracking requires manual end-to-end testing.
Output: Confirmed working phone/Viber in announcement bar, brand logos visible on home page, verified navigation, confirmed price tracking feature.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-sku-097-217-92-92-ghoud-agl/09-CONTEXT.md
@.planning/phases/09-sku-097-217-92-92-ghoud-agl/09-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Update phone and Viber in Sanity CMS and verify</name>
  <what-built>
    Research confirms:
    - Footer phone1 in messages/uk.json is already "097-217-92-92" — no code change needed.
    - AnnouncementBar phone comes from Sanity infoBar.telephone field.
    - AnnouncementBar Viber icon/link comes from Sanity infoBar.viberPhone field (or VIBER_PHONE_NUMBER env var).
    - The Viber SVG is already present in AnnouncementBar — it shows conditionally when viberPhone is set.

    What you need to do in Sanity Studio:
  </what-built>
  <how-to-verify>
    1. Open Sanity Studio at http://localhost:3333/studio (or your deployed Studio URL).
    2. Navigate to: "Налаштування сайту" (siteSettings) → find the "Info Bar" / "Announcement Bar" section.
    3. Update the "telephone" field to: 097 217 92 92
    4. Update the "Viber phone" / "viberPhone" field to: 380972179292 (international format, no + prefix, no spaces — as used by the Viber deep link)
    5. Publish the document.
    6. Check messages/ru.json Footer.phone1 — if it differs from "097-217-92-92", update it to match.
    7. Visit the site in browser. Verify the AnnouncementBar shows "097 217 92 92" and the Viber icon is visible and clickable.
  </how-to-verify>
  <resume-signal>Type "phone-done" when the Sanity update is published and phone/Viber are visible in the announcement bar.</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Add GHOUD and AGL brand logos in Sanity Studio</name>
  <what-built>
    Research confirms:
    - Brand logos live in the `brandGridBlock` in Sanity pageBuilder for the home page.
    - The BrendGrid component on the home page renders brands array — each has an image asset, collection reference, and isBrandCollection flag.
    - GHOUD and AGL collections already have isBrand: true (set in Phase Quick Task 1).
    - No code changes needed — this is pure CMS content.

    What you need to do:
  </what-built>
  <how-to-verify>
    1. Prepare logo image files for GHOUD and AGL (PNG or SVG, preferably on white or transparent background).
    2. Open Sanity Studio → navigate to the Home page in page builder.
    3. Find the "Brand Grid" block (brandGridBlock).
    4. For GHOUD:
       - Add a new brand entry (or edit existing if GHOUD is already listed without a logo).
       - Upload the GHOUD logo image as the asset.
       - Set the collection reference to the GHOUD collection.
       - Enable "isBrandCollection" if shown.
    5. Repeat for AGL.
    6. Publish the page.
    7. Visit the home page in browser. Verify GHOUD and AGL logos appear in the brand grid section.
  </how-to-verify>
  <resume-signal>Type "brands-done" when GHOUD and AGL logos are visible on the home page brand grid.</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Verify navigation menu and test "стежити за ціною" feature</name>
  <what-built>
    Research confirms:
    - Navigation reads from Shopify main menu (getMainMenu) — managed in Shopify Admin → Online Store → Navigation.
    - "Стежити за ціною" (price tracking) feature is fully implemented: PriceSubscribeModal, subscribe-price server action, ProductInfo button. Testing only required.

    What you need to do:
  </what-built>
  <how-to-verify>
    **Navigation:**
    1. Open Shopify Admin → Online Store → Navigation → Main Menu.
    2. Review the current menu structure and update any items that need changing.
    3. Save changes.
    4. Visit the site. Verify navigation menu renders as expected.

    **"Стежити за ціною" price tracking test:**
    1. Start the dev server: `npm run dev`
    2. Verify PRICE_APP_URL env var is set: `echo $PRICE_APP_URL` — should return a URL, not empty.
    3. Open any product page (e.g., http://localhost:3000/uk/product/some-handle).
    4. Click the "Стежити за ціною" button (should be visible in product info section).
    5. Modal opens — enter a test email address.
    6. Click Subscribe / confirm.
    7. Expected: Toast "Ви підписані на зміни ціни!" appears.
    8. If toast shows error: check browser network tab for the failed API call to PRICE_APP_URL/api/price-subscription. The most common cause is PRICE_APP_URL env var not set or wrong in .env.local.
    9. Report the result: working or error description.
  </how-to-verify>
  <resume-signal>Type "nav-done" and describe: (1) navigation changes made, (2) whether "стежити за ціною" works end-to-end or describe the failure if broken.</resume-signal>
</task>

</tasks>

<verification>
1. AnnouncementBar shows phone 097 217 92 92 (from Sanity).
2. AnnouncementBar shows Viber icon linking to 097 217 92 92.
3. GHOUD and AGL logos visible in home page brand grid.
4. Navigation menu updated as needed in Shopify Admin.
5. "Стежити за ціною" feature tested — result documented.
</verification>

<success_criteria>
- Phone 097 217 92 92 visible in announcement bar
- Viber icon visible and functional in announcement bar
- Brand logos (GHOUD, AGL) visible in home page brand grid
- Navigation menu verified/updated
- Price tracking feature tested and result documented
</success_criteria>

<output>
After completion, create `.planning/phases/09-sku-097-217-92-92-ghoud-agl/09-06-SUMMARY.md` with results of each human action.
</output>
