---
phase: 10-seo-fixes-update-meta-title-and-description-to-commercial-template-test-shopify-handles-fix-errors
task: heading-hierarchy-investigation
total_tasks: 3
status: in_progress
last_updated: 2026-03-05T10:06:41.553Z
---

<current_state>
Phase 10 plans (10-01, 10-02) are DONE and committed. Now investigating a NEW issue discovered during SEO audit:
incorrect heading hierarchy on product pages. Verification of phase 10 has NOT been run yet.

The heading investigation was triggered by user-provided SEO audit context. It is NOT part of the
original phase 10 plans — it needs to become a new plan or quick fix after verification.
</current_state>

<completed_work>

- Plan 10-01 ✓ COMPLETE — generateMetadata.ts updated: product/collection/brand commercial templates
  Commits: b82caa3, 2f3a9ad, fb32e8a
- Plan 10-02 ✓ COMPLETE — scripts/test-handles.mjs created (rewrote from TSV-based to Shopify-paginated)
  Commits: b82caa3, ceda267, 67e3ff0 (rewrite)
- Explored/identified heading hierarchy issues (NOT yet fixed):
  - Description.tsx line 31: `<h4>` (vendor) appears BEFORE `<h1>` (product title) — wrong order
  - ProductInfo.tsx line 144: `<h1>{product.title}</h1>` — DUPLICATE H1 (same title already in Description.tsx)
  - Content.tsx line 56-58: SheetTitle contains only `<ShoppingCart>` icon — invisible h2 in DOM

</completed_work>

<remaining_work>

1. Run phase 10 verifier (`/gsd:execute-phase 10` was interrupted before verification step)
2. Fix heading hierarchy issues found during SEO audit:
   - Description.tsx: Change `<h4>` (vendor) to `<p>` or `<span>` — vendor is not a heading
   - ProductInfo.tsx line 144: Change `<h1>` to `<p>` (Description.tsx already has the H1)
   - Content.tsx: Add text to SheetTitle or replace icon with text+icon combo
3. Confirm no other hidden headings (sr-only, etc.)

</remaining_work>

<decisions_made>

- scripts/test-handles.mjs was rewritten: source of truth is Shopify (paginate all products),
  NOT the old TSV file. Script now classifies products by vendor/productType completeness for template coverage.
- Description.tsx H1 is canonical — it's the server component. ProductInfo.tsx H1 is the duplicate.
- Phase 10 verification NOT yet run — needed before marking phase complete.

</decisions_made>

<blockers>

- Context window at 81% — stopped before fixing heading issues
- Phase 10 verifier not yet run (execute-phase was called but interrupted at wave 1 completion,
  before the verify_phase_goal step)

</blockers>

<context>
Phase 10 original scope: commercial meta templates + handle test script. Both done.
After execution, user raised additional SEO audit findings (from crawl report + screenshot):
- Duplicate page structure
- Non-ASCII in some URLs
- **Heading hierarchy broken** — H tags in DOM not matching visible content (CRITICAL)

The heading issue: Product page renders BOTH Description.tsx (h4 vendor + h1 title) AND
ProductInfo.tsx (p vendor + h1 title) — two H1s on the same page. Description.tsx is legacy;
ProductInfo.tsx is the active client component used in ProductViewProvider. Need to confirm
which one is actually rendered and remove the duplicate H1.

Key file locations:
- src/features/product/ui/Description.tsx (h4 vendor L31, h1 title L37)
- src/widgets/product-view/ui/ProductInfo.tsx (p vendor L137-140, h1 title L144)
- src/features/header/cart/ui/Content.tsx (icon-only SheetTitle L56-58)
- src/widgets/product-view/ui/ProductViewProvider.tsx (renders ProductInfo)
</context>

<next_action>
1. Check whether Description.tsx is actually rendered on the product page (it may be unused/legacy)
   → grep for `import.*Description` and `<Description` in all tsx files
2. If Description.tsx IS rendered alongside ProductInfo.tsx: remove H1 from ProductInfo.tsx (change to p)
   If Description.tsx is NOT rendered: fix its h4→p for vendor, keep h1 for title
3. Fix Content.tsx: add t('title') text to SheetTitle (commented-out version exists at line 60-62)
4. Then run phase 10 verifier
</next_action>
