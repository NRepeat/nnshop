---
phase: 08-recently-viewed-products-section-and-newsletter-subscription-section
plan: "04"
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "npm run build completes without errors"
    - "Newsletter form submits successfully and shows success state"
    - "Recently viewed carousel appears on product page after visiting products"
  artifacts: []
  key_links: []
---

<objective>
Verify the full Phase 8 build: production build passes, then a human verifies the two new UI sections work end-to-end in the browser.

Purpose: Catch any runtime issues (missing imports, wrong Suspense boundaries, form validation) before shipping. Build must pass before human verification.
Output: No new files. Go/no-go signal for the phase.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-recently-viewed-products-section-and-newsletter-subscription-section/08-CONTEXT.md
@.planning/phases/08-recently-viewed-products-section-and-newsletter-subscription-section/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full TypeScript check + production build</name>
  <files></files>
  <action>
Run full TypeScript type check and then Next.js production build:

```bash
cd /Users/mnmac/Development/nnshop
npx tsc --noEmit
npm run build
```

If TypeScript reports errors: fix them before proceeding to the build. Common issues to look for:
- Missing type imports (Product type from storefront.types)
- Async server component return type mismatches
- `useTranslations` called in a server component (should use `getTranslations` instead)
- `prisma.newsletterSubscriber` not recognized — means Prisma client needs regeneration: run `npx prisma generate`

If the build fails with Prisma client errors, run `npx prisma generate` first, then rebuild.

After a clean build, confirm these files were compiled:
- `src/entities/recently-viewed/api/record-view.ts`
- `src/entities/recently-viewed/ui/RecentlyViewedSection.tsx`
- `src/features/newsletter/ui/NewsletterForm.tsx`
- `src/widgets/home/ui/view.tsx`
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>`npm run build` exits 0 with no TypeScript or Next.js compilation errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — newsletter form and recently viewed carousel</name>
  <action>
Run `npm run dev` if the dev server is not already running, then verify the following:

**Test 1 — Newsletter section (home page):**
1. Visit http://localhost:3000/uk/woman (or any gender home page)
2. Scroll to the bottom — the newsletter section should appear above the footer
3. Verify: light/off-white background, heading text, "Для неї" radio selected by default, email input (underline-only style), black "Підписатися" button
4. Submit with an empty email — should show inline validation error (not a toast)
5. Submit a valid email — form should swap to "Дякуємо! Ви підписані" message inline

**Test 2 — Recently Viewed tracking (product page):**
1. Add a product to cart first (this triggers anonymous session creation via AddToCartButton)
2. Visit any product page: http://localhost:3000/uk/product/[any-handle]
3. Scroll down below the product info — no recently viewed section yet (first visit)
4. Visit a second product page
5. On the second product page, scroll down — "Нещодавно переглянуті" carousel should appear showing the first product

**Test 3 — Recently viewed on home page:**
1. After visiting 2+ products (with a session), go back to the home page
2. Scroll below the hero/product carousels — "Нещодавно переглянуті" section should appear

Note: If no recently viewed section appears, check server logs for `[recordProductView]` entries. The section is hidden if no session exists (user has not added to cart yet).
  </action>
  <verify>Human confirms both sections work as described above</verify>
  <done>Newsletter form shows success state after submission; recently viewed carousel appears after browsing 2+ products with a session</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- Newsletter form on home page shows success state after valid email submission
- Recently viewed carousel appears on product page after visiting 2+ products with a session
</verification>

<success_criteria>
- Production build passes without errors
- Newsletter section visible on home page with correct styling and working form submission
- Recently viewed carousel visible after browsing products with an active session
</success_criteria>

<output>
After completion, create `.planning/phases/08-recently-viewed-products-section-and-newsletter-subscription-section/08-04-SUMMARY.md`
</output>
