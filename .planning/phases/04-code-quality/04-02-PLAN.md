---
phase: 04-code-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/home/ui/HeroPageBuilder.tsx
  - src/widgets/hero-swiper/ui/HeroSwiper.tsx
autonomous: true
requirements:
  - TYPE-01

must_haves:
  truths:
    - "HeroPageBuilder switch-cases use Sanity-typed assertions, not `as any`"
    - "HeroSwiper accepts only the props it actually uses"
    - "`npm run build` produces no TypeScript errors in either file"
  artifacts:
    - path: "src/features/home/ui/HeroPageBuilder.tsx"
      provides: "Block-type assertions using Faqs, CollectionsCarousel, SliderBlock from @shared/sanity/types"
      contains: "import type { Faqs, CollectionsCarousel, SliderBlock }"
    - path: "src/widgets/hero-swiper/ui/HeroSwiper.tsx"
      provides: "Props narrowed to only slides (removes unused documentId/documentType/blockIndex)"
      contains: "slides"
  key_links:
    - from: "src/features/home/ui/HeroPageBuilder.tsx"
      to: "src/shared/sanity/types.ts"
      via: "named type imports"
      pattern: "as Faqs|as CollectionsCarousel|as SliderBlock"
    - from: "src/features/home/ui/HeroPageBuilder.tsx"
      to: "src/widgets/hero-swiper/ui/HeroSwiper.tsx"
      via: "slides prop pass-through"
      pattern: "slides=\\{.*block.*slides\\}"
---

<objective>
Remove all `as any` casts from HeroPageBuilder by importing named Sanity-generated types and using explicit type assertions in each switch case. Simultaneously narrow HeroSwiper's props to remove the three unused fields (documentId, documentType, blockIndex) that prevent a clean spread.

Purpose: The block switch is the most cast-heavy file in the phase — 5 separate `as any` instances. Fixing it requires coordinating HeroPageBuilder and HeroSwiper together since the SliderBlock case depends on HeroSwiper's prop signature.
Output: HeroPageBuilder with zero `as any`, HeroSwiper with minimal typed props, both compiling cleanly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality/04-CONTEXT.md
@.planning/phases/04-code-quality/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow HeroSwiper props to slides only</name>
  <files>src/widgets/hero-swiper/ui/HeroSwiper.tsx</files>
  <action>
Read the current file first. Also read `src/shared/sanity/types.ts` to confirm the `SliderBlock` type shape.

The current `HeroSwiperProps` likely extends `Extract<PAGE_QUERYResult['content'][number], { _type: 'sliderBlock' }>` or a similar full-block type that includes `documentId`, `documentType`, and `blockIndex`. These three fields are never read inside the component body.

Fix:
1. Import `SliderBlock` from `@shared/sanity/types`
2. Redefine `HeroSwiperProps` as:
   ```typescript
   interface HeroSwiperProps {
     slides: SliderBlock['slides'];
   }
   ```
   (or `type HeroSwiperProps = { slides: SliderBlock['slides'] }` — use whichever form the file currently uses)
3. Remove destructuring of `documentId`, `documentType`, `blockIndex` from the component's parameter list if present
4. Keep all other props and logic unchanged

Do not change carousel logic, slide rendering, or any other behavior.
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "HeroSwiper" || echo "No HeroSwiper errors"</automated>
    <manual>Confirm HeroSwiperProps no longer references documentId, documentType, or blockIndex</manual>
  </verify>
  <done>HeroSwiper.tsx compiles cleanly. HeroSwiperProps requires only `slides`. No `documentId`, `documentType`, or `blockIndex` in the props interface.</done>
</task>

<task type="auto">
  <name>Task 2: Replace HeroPageBuilder as-any casts with Sanity type assertions</name>
  <files>src/features/home/ui/HeroPageBuilder.tsx</files>
  <action>
Read the current file first. Read `src/shared/sanity/types.ts` to confirm available types if not already loaded.

There are five `as any` usages in this file across the switch-case block rendering. Fix each:

**Import at top of file** (add to existing imports or add new import):
```typescript
import type { Faqs, CollectionsCarousel, SliderBlock } from '@shared/sanity/types';
```

**`case 'faqs':`**
Replace `{...(block as any)}` spread with `{...(block as Faqs)}`.
The block after `case 'faqs':` is already the faqs union member; the `as Faqs` assertion makes it explicit.

**`case 'collectionsCarousel':`**
Replace the three `as any` casts (`block.collections as any`, `block.title as any`, `block.action_text as any`) by asserting the block first:
```typescript
const carousel = block as CollectionsCarousel;
```
Then pass `collections={carousel.collections}`, `title={carousel.title}`, `action_text={carousel.action_text}` — no `as any` on individual props.

**`case 'sliderBlock':`**
Replace `{...(block as any)}` or `block.slides as any` with:
```typescript
slides={(block as SliderBlock).slides}
```
(HeroSwiper now only requires `slides` after Task 1.)

**`case 'similarProducts':`**
Replace `block.collection as any` with `(block as SimilarProducts).collection` — OR, if TypeScript narrows correctly after the case, simply `block.collection` without any cast. Import `SimilarProducts` from `@shared/sanity/types` if needed.

**`default:` case**
Replace `(block as any)._type` with `(block as { _type: string })._type` for the console.warn logging argument.

Do not change rendering logic, key props, gender forwarding, or any other behavior. Only remove the `as any` casts.
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "HeroPageBuilder" || echo "No HeroPageBuilder errors"</automated>
    <manual>Confirm no `as any` remains: grep -c "as any" src/features/home/ui/HeroPageBuilder.tsx — should return 0</manual>
  </verify>
  <done>HeroPageBuilder.tsx imports Faqs, CollectionsCarousel, SliderBlock from @shared/sanity/types. Zero `as any` in the file. TypeScript reports no errors for either HeroPageBuilder.tsx or HeroSwiper.tsx.</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -c "as any" src/features/home/ui/HeroPageBuilder.tsx` returns `0`
- `grep -c "as any" src/widgets/hero-swiper/ui/HeroSwiper.tsx` returns `0`
- `npx tsc --noEmit` reports no errors in either file
</verification>

<success_criteria>
- HeroPageBuilder.tsx: zero `as any`, named Sanity type assertions in each case, compiles cleanly
- HeroSwiper.tsx: props narrowed to `{ slides: SliderBlock['slides'] }`, compiles cleanly
- No runtime behavior change — slides still render, carousels still function
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality/04-02-SUMMARY.md`
</output>
