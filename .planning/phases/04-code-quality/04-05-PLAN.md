---
phase: 04-code-quality
plan: 05
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
  - "04-04"
files_modified: []
autonomous: false
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - MEM-01
  - MEM-02

must_haves:
  truths:
    - "`npm run build` exits zero with no TypeScript errors"
    - "All target files contain zero `as any` or `Record<string, any>` casts"
    - "All four setTimeout components have clearTimeout cleanup"
    - "User has confirmed the app builds and loads without errors"
  artifacts:
    - path: "src/features/checkout/delivery/ui/UkrPoshtaForm.tsx"
      provides: "Verified zero as-any"
      contains: "useFormContext<DeliveryInfo>"
    - path: "src/features/home/ui/HeroPageBuilder.tsx"
      provides: "Verified zero as-any"
      contains: "as Faqs"
    - path: "src/entities/announcement-bar/announcement-bar.tsx"
      provides: "Verified zero as-any"
      contains: "typeof text === 'string'"
    - path: "src/entities/cart/api/anonymous-cart-buyer-identity-update.ts"
      provides: "Verified zero Record<string, any>"
      contains: "AnonymousUserArg"
  key_links:
    - from: "npm run build"
      to: "TypeScript compiler"
      via: "tsconfig.json"
      pattern: "exit 0"
---

<objective>
Run a full `npm run build` to confirm all Phase 4 type and memory changes compile cleanly. Then verify the target files contain no remaining unsafe casts and all setTimeout components have cleanup. A human-verify checkpoint confirms the build passes before the phase is marked complete.

Purpose: All four Wave 1 plans run independently in parallel — this Wave 2 plan is the integration gate that confirms they produce a coherent, compiling codebase.
Output: Confirmed zero-error build; Phase 4 complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality/04-01-SUMMARY.md
@.planning/phases/04-code-quality/04-02-SUMMARY.md
@.planning/phases/04-code-quality/04-03-SUMMARY.md
@.planning/phases/04-code-quality/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and cast audit</name>
  <files></files>
  <action>
Run the following checks in sequence. Record results for the SUMMARY.

**Step 1 — Full production build:**
```bash
npm run build
```
This runs `prisma generate` then the Next.js build with TypeScript checking. The build must exit 0 with no type errors.

**Step 2 — as-any audit on TYPE-01 files:**
```bash
grep -rn "as any" \
  src/features/checkout/delivery/ui/UkrPoshtaForm.tsx \
  src/features/home/ui/HeroPageBuilder.tsx \
  src/entities/announcement-bar/announcement-bar.tsx
```
Expected: no output (zero matches).

**Step 3 — Record-any audit on TYPE-02 file:**
```bash
grep -n "Record" src/entities/cart/api/anonymous-cart-buyer-identity-update.ts
```
Expected: no output (zero matches).

**Step 4 — setTimeout ref audit on MEM-02 files:**
```bash
grep -rn "clearTimeout" \
  src/features/novaPoshta/ui/NovaPoshtaButton.tsx \
  src/features/product/quick-buy/ui/QuickBuyModal.tsx \
  src/features/header/language-switcher/ui/LanguageSwitcher.tsx \
  src/entities/home/ui/SyncedCarousels.tsx
```
Expected: 4 matches (one per file).

If any check reveals a remaining issue, fix it inline before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npm run build 2>&1 | tail -20</automated>
    <manual>Confirm build output shows "Compiled successfully" or equivalent zero-error output</manual>
  </verify>
  <done>`npm run build` exits 0. All four grep audits return expected results (zero as-any, zero Record-any, four clearTimeout matches).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — build passes and app loads correctly</name>
  <action>Pause for human verification of build output and app functionality.</action>
  <what-built>
Phase 4 Code Quality complete. All Wave 1 plans have run:
- Plan 01: UkrPoshtaForm and AnnouncementBar — as-any removed
- Plan 02: HeroPageBuilder and HeroSwiper — as-any removed, HeroSwiper props narrowed
- Plan 03: anonymous-cart-buyer-identity-update — Record&lt;string, any&gt; removed; TYPE-03 and MEM-01 verified as already satisfied
- Plan 04: NovaPoshtaButton, QuickBuyModal, LanguageSwitcher, SyncedCarousels — setTimeout refs added with cleanup
  </what-built>
  <how-to-verify>
1. Confirm `npm run build` completed in Task 1 with zero TypeScript errors
2. Start dev server: `npm run dev`
3. Visit the home page — confirm the hero carousel and collections carousel render correctly
4. Visit a checkout page — confirm the delivery form (UkrPoshtaForm) renders without errors
5. Open a product quick-buy modal — confirm it still opens and closes correctly
6. Check the announcement bar — confirm text displays (not blank)
7. Check the browser console for any new runtime errors
  </how-to-verify>
  <verify>
    <automated>MISSING — visual verification required</automated>
    <manual>User confirms app loads without errors and key UI flows work correctly</manual>
  </verify>
  <done>User has typed "approved" confirming the build passes and the app loads correctly.</done>
  <resume-signal>Type "approved" if the build passes and the app loads correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- Zero `as any` in all TYPE-01 target files
- Zero `Record&lt;string, any&gt;` in TYPE-02 target file
- `clearTimeout` present in all four MEM-02 target files
- Human confirmed app loads and key UI flows work
</verification>

<success_criteria>
- Full Next.js production build passes with zero TypeScript errors under current tsconfig
- All Phase 4 requirements (TYPE-01, TYPE-02, TYPE-03, MEM-01, MEM-02) verified as satisfied
- No runtime regressions in hero carousel, delivery form, quick-buy modal, or announcement bar
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality/04-05-SUMMARY.md`
</output>
