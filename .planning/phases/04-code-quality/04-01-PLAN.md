---
phase: 04-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/checkout/delivery/ui/UkrPoshtaForm.tsx
  - src/entities/announcement-bar/announcement-bar.tsx
autonomous: true
requirements:
  - TYPE-01

must_haves:
  truths:
    - "UkrPoshtaForm renders without any `as any` cast in field name props"
    - "AnnouncementBar renders text without double-cast `as any as string`"
    - "`npm run build` produces no TypeScript errors in either file"
  artifacts:
    - path: "src/features/checkout/delivery/ui/UkrPoshtaForm.tsx"
      provides: "Typed form context using DeliveryInfo; field array items typed as Path<DeliveryInfo>"
      contains: "useFormContext<DeliveryInfo>"
    - path: "src/entities/announcement-bar/announcement-bar.tsx"
      provides: "String narrowing via typeof guard; no double cast"
      contains: "typeof text === 'string'"
  key_links:
    - from: "src/features/checkout/delivery/ui/UkrPoshtaForm.tsx"
      to: "src/features/checkout/delivery/model/deliverySchema.ts"
      via: "DeliveryInfo import"
      pattern: "useFormContext<DeliveryInfo>"
    - from: "src/entities/announcement-bar/announcement-bar.tsx"
      to: "HEADER_QUERYResult"
      via: "displayText narrowing"
      pattern: "typeof text === 'string'"
---

<objective>
Remove `as any` type casts from UkrPoshtaForm and AnnouncementBar. Both files have narrow, well-understood fixes: UkrPoshtaForm needs a typed form context generic; AnnouncementBar needs a runtime string guard instead of a double cast.

Purpose: Eliminate unsafe escape hatches so TypeScript can catch field-name typos and type mismatches at compile time.
Output: Two files with zero `as any` casts, verified by `npm run build`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality/04-CONTEXT.md
@.planning/phases/04-code-quality/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type UkrPoshtaForm with DeliveryInfo generic</name>
  <files>src/features/checkout/delivery/ui/UkrPoshtaForm.tsx</files>
  <action>
Read the current file first.

Fix `as any` cast in the form field name prop by typing the form context with the concrete schema type:

1. Add import: `import { DeliveryInfo } from '../model/deliverySchema';`
2. Add import (or add to existing react-hook-form import): `import { useFormContext, Path } from 'react-hook-form';`
3. Define a private (unexported) type directly above the field arrays:
   ```typescript
   type FieldConfig = { name: Path<DeliveryInfo>; label: string; placeholder: string };
   ```
4. Annotate both field arrays (`addressFields` and `locationFields`, or whatever they are named) with `: FieldConfig[]`
5. Change `useFormContext()` to `useFormContext<DeliveryInfo>()`
6. Remove the `as any` cast from the `name={item.name as any}` prop — it is now typed as `Path<DeliveryInfo>`

Do not export `FieldConfig`. Do not change any other logic, JSX structure, or imports.
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "UkrPoshtaForm" || echo "No UkrPoshtaForm errors"</automated>
    <manual>Confirm no `as any` remains in UkrPoshtaForm.tsx</manual>
  </verify>
  <done>UkrPoshtaForm.tsx contains `useFormContext&lt;DeliveryInfo&gt;()`, a `FieldConfig` type with `name: Path&lt;DeliveryInfo&gt;`, and zero `as any` casts. TypeScript reports no errors for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow AnnouncementBar text with typeof guard</name>
  <files>src/entities/announcement-bar/announcement-bar.tsx</files>
  <action>
Read the current file first.

The `text` variable from the Sanity query has type `Array<{...}> | string | ''` — the TypeGen includes an array branch even though the GROQ coalesce always returns a string at runtime. Two `{text as any as string}` double-casts exist (one for mobile, one for desktop rendering).

Fix:
1. After destructuring or receiving `text` from the Sanity query result, add a narrowing assignment directly before the return statement:
   ```typescript
   const displayText = typeof text === 'string' ? text : '';
   ```
2. Replace both occurrences of `{text as any as string}` with `{displayText}`

Do not change any other logic, layout, or Sanity query. Do not create a new type or interface — the `typeof` guard is sufficient.
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "announcement-bar" || echo "No announcement-bar errors"</automated>
    <manual>Confirm no `as any` remains in announcement-bar.tsx</manual>
  </verify>
  <done>announcement-bar.tsx renders `{displayText}` (not `{text as any as string}`) in both mobile and desktop paths. TypeScript reports no errors for this file.</done>
</task>

</tasks>

<verification>
After both tasks: `grep -r "as any" src/features/checkout/delivery/ui/UkrPoshtaForm.tsx src/entities/announcement-bar/announcement-bar.tsx` returns empty. `npm run build` completes without TypeScript errors in either file.
</verification>

<success_criteria>
- UkrPoshtaForm.tsx: zero `as any`, `useFormContext<DeliveryInfo>()` present, field arrays typed as `FieldConfig[]`
- announcement-bar.tsx: zero `as any`, `displayText` narrowing present, `{displayText}` used in JSX
- Both files compile cleanly under current tsconfig
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality/04-01-SUMMARY.md`
</output>
