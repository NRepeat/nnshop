---
phase: 04-code-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/cart/api/anonymous-cart-buyer-identity-update.ts
autonomous: true
requirements:
  - TYPE-02
  - TYPE-03
  - MEM-01

must_haves:
  truths:
    - "`Record<string, any>` intersection removed from anonymousCartBuyerIdentityUpdate parameter types"
    - "Inline unexported interfaces AnonymousUserArg and NewUserArg describe the function's parameter shape"
    - "TYPE-03 (Promise.allSettled named destructuring in auth.ts) already satisfied by Phase 2"
    - "MEM-01 (addEventListener cleanup in NovaPoshtaButton) already satisfied by existing removeEventListener"
    - "`npm run build` produces no TypeScript errors in anonymous-cart-buyer-identity-update.ts"
  artifacts:
    - path: "src/entities/cart/api/anonymous-cart-buyer-identity-update.ts"
      provides: "Inline interfaces AnonymousUserArg and NewUserArg replacing Record<string, any>"
      contains: "interface AnonymousUserArg"
  key_links:
    - from: "src/entities/cart/api/anonymous-cart-buyer-identity-update.ts"
      to: "better-auth User/Session types"
      via: "AnonymousUserArg and NewUserArg inline interfaces"
      pattern: "interface AnonymousUserArg|interface NewUserArg"
---

<objective>
Replace `Record<string, any>` intersections in the `anonymousCartBuyerIdentityUpdate` function parameters with named inline interfaces. Also confirm (by code inspection) that TYPE-03 and MEM-01 are already satisfied so they do not require code changes.

Purpose: The `T & Record<string, any>` intersection defeats TypeScript's exhaustive checks on the parameter shape. Replacing it with explicit interfaces tightens the contract without changing runtime behavior.
Output: One file updated; TYPE-03 and MEM-01 verified as already satisfied.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality/04-CONTEXT.md
@.planning/phases/04-code-quality/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Record&lt;string, any&gt; with inline interfaces in cart buyer identity update</name>
  <files>src/entities/cart/api/anonymous-cart-buyer-identity-update.ts</files>
  <action>
Read the current file first.

The function `anonymousCartBuyerIdentityUpdate` has a parameter destructure shape where `anonymousUser` and `newUser` each have `user` and `session` typed with `& Record<string, any>` intersections. The function body only accesses `user.id`, `user.email`, `user.isAnonymous` (all on the base types); session fields are never accessed.

Fix:
1. Directly above the `anonymousCartBuyerIdentityUpdate` function definition, add two unexported inline interfaces:
   ```typescript
   interface AnonymousUserArg {
     user: UserWithAnonymous;
     session: Session;
   }

   interface NewUserArg {
     user: User;
     session: Session;
   }
   ```
   Use `interface` (not `type`) per the pattern. Do not export them.

2. Update the function parameter type to use these interfaces:
   ```typescript
   export const anonymousCartBuyerIdentityUpdate = async ({
     newUser,
     anonymousUser,
   }: {
     anonymousUser: AnonymousUserArg;
     newUser: NewUserArg;
   }) => {
   ```

3. Remove the `& Record<string, any>` from both `user` and `session` fields in the existing parameter type.

4. Confirm that `UserWithAnonymous`, `User`, and `Session` are already imported in this file. If not, add the missing imports from their respective better-auth sources (check existing imports for the pattern used in this file).

Do not change any function body logic, imports (beyond adding any missing type imports), or other exports.
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "anonymous-cart-buyer-identity-update" || echo "No errors in anonymous-cart-buyer-identity-update"</automated>
    <manual>Confirm no `Record&lt;string, any&gt;` remains: grep "Record" src/entities/cart/api/anonymous-cart-buyer-identity-update.ts — should return empty</manual>
  </verify>
  <done>anonymous-cart-buyer-identity-update.ts contains `AnonymousUserArg` and `NewUserArg` interfaces directly above the function. Zero `Record&lt;string, any&gt;` in the file. TypeScript reports no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify TYPE-03 and MEM-01 already satisfied</name>
  <files>src/features/auth/lib/auth.ts, src/features/novaPoshta/ui/NovaPoshtaButton.tsx</files>
  <action>
Read both files.

**TYPE-03 verification:** In `src/features/auth/lib/auth.ts`, locate the `Promise.allSettled` call in the account linking / `linkAnonymousDataToUser` function. Confirm the result is destructured by name (e.g., `const [favResult, cartResult] = ...`) rather than accessed by index (e.g., `results[0]`, `results[1]`). Phase 2 plan 02-03 fixed this. If already using named destructuring — no code change needed.

**MEM-01 verification:** In `src/features/novaPoshta/ui/NovaPoshtaButton.tsx`, locate the `window.addEventListener('message', handleFrameMessage)` call. Confirm a matching `window.removeEventListener('message', handleFrameMessage)` exists in the `useEffect` return cleanup. Research confirmed this is already present. If already present — no code change needed.

If either is NOT already fixed (i.e., research was incorrect), fix it inline:
- TYPE-03: Change index access `results[0]` / `results[1]` to `const [nameA, nameB] = results` with meaningful names
- MEM-01: Add `return () => { window.removeEventListener('message', handleFrameMessage); }` to the relevant useEffect

In the SUMMARY, explicitly record that TYPE-03 and MEM-01 were verified as already satisfied (or document the fix if a code change was needed).
  </action>
  <verify>
    <automated>cd /Users/mnmac/Development/nnshop && grep -n "allSettled" src/features/auth/lib/auth.ts && grep -n "removeEventListener" src/features/novaPoshta/ui/NovaPoshtaButton.tsx</automated>
    <manual>Confirm named destructuring in auth.ts and removeEventListener in NovaPoshtaButton.tsx are present</manual>
  </verify>
  <done>TYPE-03: auth.ts uses named destructuring for Promise.allSettled results (not array index access). MEM-01: NovaPoshtaButton.tsx has `removeEventListener` in useEffect cleanup. Both requirements satisfied — confirmed by grep.</done>
</task>

</tasks>

<verification>
- `grep "Record" src/entities/cart/api/anonymous-cart-buyer-identity-update.ts` returns empty
- `grep -n "allSettled" src/features/auth/lib/auth.ts` shows named destructuring
- `grep -n "removeEventListener" src/features/novaPoshta/ui/NovaPoshtaButton.tsx` shows cleanup
- `npx tsc --noEmit` reports no errors in anonymous-cart-buyer-identity-update.ts
</verification>

<success_criteria>
- anonymous-cart-buyer-identity-update.ts: zero `Record&lt;string, any&gt;`, two inline interfaces added, compiles cleanly
- auth.ts: Promise.allSettled results use named destructuring (TYPE-03 satisfied)
- NovaPoshtaButton.tsx: removeEventListener exists in useEffect cleanup (MEM-01 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality/04-03-SUMMARY.md`
</output>
